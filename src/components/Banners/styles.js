import { StyleSheet } from "react-native";
import { themes } from "../../themes";

export const styles = StyleSheet.create({
  
    banners: {
      marginTop: -18
    },

    bannerItem: {
      width: 370,
      height: 210,
      marginRight: 20,
      borderRadius: 5,
    },
});